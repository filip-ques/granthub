{
  "name": "granthub",
  "version": "1.0.0",
  "description": "GrantHub — granty, dotácie a verejné zákazky na jednom mieste",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js",
    "seed": "node --env-file-if-exists=.env src/seed.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@cloudflare/containers": "^0.3.7",
    "cheerio": "^1.2.0",
    "connect-pg-simple": "^10.0.0",
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "nodemailer": "^9.0.3",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
